{
  "name": "vibe_coding",
  "version": "1.0.0",
  "description": "The website is backed by a **local SQLite database** via a tiny Node API server (zero dependencies, uses Node's built-in `node:sqlite`).",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
